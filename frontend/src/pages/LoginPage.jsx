// TODO: Build login form with email + password fields
// TODO: On submit, call authService.login(credentials) and store result in AuthContext
// TODO: Show validation errors inline
// TODO: Redirect to /dashboard on success
// TODO: Show error message on failed login

function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
        <h1 className="text-2xl font-bold text-center mb-6">Sign In</h1>
        {/* TODO: Add email input */}
        {/* TODO: Add password input */}
        {/* TODO: Add submit button */}
        {/* TODO: Add link to /register */}
      </div>
    </div>
  )
}

export default LoginPage
