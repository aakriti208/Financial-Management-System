import api from '../api/axiosInstance'

/**
 * Get predicted expenses for next month.
 * TODO: GET /prediction/expenses — returns array of ExpenseDTO (predicted)
 */
export const getExpensePrediction = async () => {
  // TODO: const response = await api.get('/prediction/expenses')
  // TODO: return response.data
  return null
}
