import api from '../api/axiosInstance'

/**
 * Calculate tuition cost.
 * TODO: POST /tuition/calculate with { tuitionPerCourse, numberOfCourses, scholarshipAmount }
 * TODO: Returns TuitionResultDTO: { grossTuition, scholarshipAmount, netTuition, ... }
 */
export const calculate = async (tuitionData) => {
  // TODO: const response = await api.post('/tuition/calculate', tuitionData)
  // TODO: return response.data
  return null
}
